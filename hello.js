function sayHello (name) {
  console.log("Hello ", name);
}

sayHello("Frau");
sayHello("Scott");
sayHello("Number 2");

/* This kind of function produces a side effect,
as in it does something. The other main type is a function that
produces a result, as in it calculates and returns a value we can use
in further code. One such function could be for determining whether
a number is even. Hint: Let's do that next. */

